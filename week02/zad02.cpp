#include <iostream>

using namespace std;
int main()
{
    int month;
    cin >> month;
    if (month >= 1 && month <=12)
    {
        cout << "Valid month";
    }
    else
    {
        cout << "Unvalid month";

    }

    
}
